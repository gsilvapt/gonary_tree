Sample implementation of a binary tree structure in Go, that would allow to perform 
binary tree searching in a golang project.

Simple project to learn Go + this structure, nothing serious to see here.